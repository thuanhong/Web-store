<!DOCTYPE html>
<html lang="en">
<% include ../layouts/head %>
<body>
<% include ../layouts/header %>
<main>
    <div class="w3-half w3-display-middle w3-mobile w3-card-4">
        <div class="w3-container w3-blue">
            <h2><%= action %>product</h2>
        </div>
        <form class="w3-container" action="/admin/<% if (editing) { %>edit-product/<%= product._id %><% } else { %>add-product<% } %>" method="POST">
            <div class="w3-section">
                <label class="w3-text-blue">Title</label>
                <input class="w3-input w3-border" type="text" name="title" value="<% if (editing) {%><%= product.title %><% } %>" required>
            </div>
            <div class="w3-section">
                <label class="w3-text-blue">Price</label>
                <input class="w3-input w3-border" type="text" name="price" value="<% if (editing) {%><%= product.price %><% } %>" required>
            </div>
            <div class="w3-section">
                <label class="w3-text-blue">Image URL</label>
                <input class="w3-input w3-border" type="text" name="url" value="<% if (editing) {%><%= product.imageURL %><% } %>" required>
            </div>
            <div class="w3-section">
                <label class="w3-text-blue">Description</label>
                <textarea rows="4" cols="50" class="w3-input w3-border" type="text" name="description" required><% if (editing) {%><%= product.description %><% } %></textarea>
            </div>
            <div class="w3-section">
                <button class="w3-button w3-blue">Submit</button>
            </div>
        </form>
    </div>
</main>
<% include ../layouts/footer %>
</body>
</html>
