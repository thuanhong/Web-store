<!DOCTYPE html>
<html>
    <% include ../layouts/head %>
    <body>
        <% include ../layouts/header %>
        <main>
            <div class="w3-half w3-display-middle w3-mobile w3-card-4">
                <div class="w3-container w3-blue">
                    <h2>New Password</h2>
                </div>
                <form class="w3-container" action="/auth/reset-password/<%= userToken %>" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
                    <input type="hidden" name="userId" value="<%= userId %>"/>
                    <div class="w3-section">
                        <label class="w3-text-blue">Password</label>
                        <input id="password" class="w3-input w3-border" type="password" name="password" pattern=".{8,}" title="Must contain at least 8 or more characters" required>
                    </div>
                    <div class="w3-section">
                        <label class="w3-text-blue">Confrim Password</label>
                        <input id="confirm_password" class="w3-input w3-border" type="password" name="confirmPassword" required>
                    </div>
                    <div class="w3-section">
                        <button class="w3-button w3-blue">Cofirm new password</button>
                    </div>
                </form>
            </div>
        </main>
        <% include ../layouts/footer %>
    </body>
</html>