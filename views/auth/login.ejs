<!DOCTYPE html>
<html lang="en">
    <% include ../layouts/head %>
    <body>
        <% include ../layouts/header %>

        <main>
            <div class="w3-half w3-display-middle w3-mobile w3-card-4">
                <% if (show) { %>
                    <% if (show[0] === 'Y') { %>
                        <div class="w3-panel w3-leftbar w3-pale-red w3-border-red" >
                            <p><%= show %></p>
                        </div>
                    <% } else { %>
                        <div class="w3-panel w3-leftbar w3-pale-green w3-border-green" >
                            <p><%= show %></p>
                        </div>
                    <% } %>
                <% } %>
                <div class="w3-blue">
                    <div class="w3-row">
                        <h2 class="w3-half tablink w3-bottombar w3-hover-light-grey w3-padding w3-border-red" onclick="changeTab(this, 'login')">Login</h2>
                        <h2 class="w3-half tablink w3-bottombar w3-hover-light-grey w3-padding" onclick="changeTab(this, 'signup')">Sign Up</h2>
                    </div>
                </div>

                <form id="login" class="w3-container tab" action="/auth/login" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
                    <div class="w3-section">
                        <label class="w3-text-blue">Email</label>
                        <input class="w3-input w3-border" type="text" name="email" required>
                    </div>
                    <div class="w3-section">
                        <label class="w3-text-blue">Password</label>
                        <input class="w3-input w3-border" type="password" name="password" required>
                    </div>
                    <div class="w3-section">
                        <button class="w3-button w3-blue">Login</button>
                    </div>
                </form>

                <form id='signup' class="w3-container tab" action="/auth/signup" method="POST" style="display: none">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
                    <div class="w3-section">
                        <label class="w3-text-blue">User name</label>
                        <input class="w3-input w3-border" type="text" name="username" required>
                    </div>
                    <div class="w3-section">
                        <label class="w3-text-blue">Email</label>
                        <input class="w3-input w3-border" type="email" name="email" required>
                    </div>
                    <div class="w3-section">
                        <label class="w3-text-blue">Password</label>
                        <input id="password" class="w3-input w3-border" type="password" name="password" pattern=".{8,}" title="Must contain at least 8 or more characters" required>
                    </div>
                    <div class="w3-section">
                        <label class="w3-text-blue">Confrim Password</label>
                        <input id="confirm_password" class="w3-input w3-border" type="password" name="confirmPassword" required>
                    </div>
                    <div class="w3-section">
                        <button class="w3-button w3-blue">Sign up</button>
                    </div>
                </form>
            </div>
        </main>
        <% include ../layouts/footer %>
    </body>
</html>